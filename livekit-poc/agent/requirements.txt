# Python 3.10 - 3.12（不要用 3.13+，部分依赖未跟上）
livekit-agents[openai,silero,turn-detector]~=1.2
livekit-api~=1.0
python-dotenv~=1.0
# 外呼 HTTP 接口
fastapi~=0.115
uvicorn~=0.32
# 垫词音频生成（可选，只在跑 gen_fillers.py 时需要）
dashscope>=1.23
