# ===== LiveKit（与 deploy/livekit.yaml keys 一致）=====
LIVEKIT_URL=ws://127.0.0.1:7880
LIVEKIT_API_KEY=APIxxxxxxxx
LIVEKIT_API_SECRET=REPLACE_WITH_RANDOM_SECRET

# ===== 外呼 trunk（scripts/create_trunk.sh 输出的 ST_xxx）=====
SIP_OUTBOUND_TRUNK_ID=ST_xxxxxxxx

# ===== Qwen Omni Realtime（阿里云百炼）=====
DASHSCOPE_API_KEY=sk-REPLACE
QWEN_REALTIME_URL=wss://dashscope.aliyuncs.com/api-ws/v1/realtime
QWEN_REALTIME_MODEL=qwen3-omni-flash-realtime
QWEN_VOICE=Cherry

# ===== 通话行为 =====
# 商家名等业务上下文由 dial 接口按通传入，这里是默认值
MERCHANT_NAME_DEFAULT=您的门店
# 服务端 VAD 静音判停毫秒数（PoC 调参重点：300-500 之间试）
VAD_SILENCE_MS=400
# 垫词开关（assets/ 下需有 filler_*.wav，可用 scripts/gen_fillers.py 生成）
ENABLE_FILLER=true

# ===== 延迟打点输出目录 =====
METRICS_DIR=./metrics_logs

# ===== 防封卡策略（dial_policy.py，2026-07-08 SIM 被风控停机的教训）=====
# 状态库路径。与 backend 同机部署时指到同一个绝对路径，两边共享额度。
DIAL_POLICY_DB=./dial_policy.sqlite3
# SIM 口列表，逗号分隔。UC100 单卡保持一个即可；8口鼎信按口配 8 个名字自动轮换。
DIAL_PORTS=uc100
# 单卡滚动一小时上限（事故当天 10+ 通/时触发风控，默认留一半余量）
DIAL_PORT_HOURLY_CAP=6
# 单卡自然日上限
DIAL_PORT_DAILY_CAP=50
# 同一被叫号码：最小间隔秒数 / 每日上限
DIAL_SAME_NUMBER_MIN_INTERVAL_S=1800
DIAL_SAME_NUMBER_DAILY_CAP=3
# 同卡两通之间的随机间隔区间（秒）
DIAL_GAP_MIN_S=60
DIAL_GAP_MAX_S=150
# 紧急逃生口（真机终验当天如需临时放宽，优先调上面的参数而不是关掉它）
DIAL_POLICY_ENABLED=true
